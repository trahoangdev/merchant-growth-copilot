{
  "name": "merchant-growth-copilot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "build:client": "npm --prefix client run build",
    "typecheck:client": "npm --prefix client run typecheck",
    "dev:server": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir server",
    "check:server": "python -m compileall server\\app"
  }
}
